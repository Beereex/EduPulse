import 'package:flutter/material.dart';

class EditAccount extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Edit Account'),
      ),
      body: Center(
        child: Text('Edit Account Page Content'),
      ),
    );
  }
}
